<config>

  <param name="threadPool.on">true</param>
  <param name="dev.mode">false</param>
  <param name="bam.enabled">false</param>

  <param name="ksb.org.quartz.jobStore.tablePrefix">QRTZ_</param>

  <param name="transaction.timeout">${transaction.timeout}</param>

  <param name="plugin.dir">/opt/j2ee/en-plugins/${environment}</param>

  <param name="data.xml.root.location">/opt/ears/${environment}/en/xml</param>
  <param name="data.xml.pending.location">${data.xml.root.location}pending</param>
  <param name="data.xml.loaded.location">${data.xml.root.location}loaded</param>
  <param name="data.xml.problem.location">${data.xml.root.location}problem</param>
  <param name="data.xml.pollIntervalSecs">30</param>
  <param name="initialDelaySecs">10</param>

  <param name="datasource.platform">${rice.workflow.datasource.platform}</param>
  <param name="datasource.ojb.platform">${datasource.ojb.platform}</param>
  <param name="datasource.ojb.sequenceManager">${datasource.ojb.sequence.manager}</param>
  <param name="datasource.driver.name">${kew.driver.class}</param>
  <param name="datasource.url">${kew.datasource.url}</param>
  <param name="datasource.username">${kew.datasource.username}</param>
  <param name="datasource.password">${kew.datasource.password}</param>
  <param name="datasource.pool.minSize">2</param>
  <param name="datasource.pool.maxSize">50</param>
  <param name="datasource.pool.maxWait">10000</param>
  <param name="datasource.pool.validationQuery">select 1 from dual</param>

  <param name="attachment.dir.location">${attachments.directory}</param>

  <param name="log4j.settings.path">${log4j.settings.file}</param>
  <param name="log4j.settings.reloadInterval">${log4j.reload.minutes}</param>

  <param name="filter.cas.class">org.kuali.core.web.filter.KualiCasFilter</param>
  <param name="filter.cas.edu.yale.its.tp.cas.client.filter.loginUrl">${application.url}/${cas.application}/login</param>
  <param name="filter.cas.edu.yale.its.tp.cas.client.filter.validateUrl">${application.url}/${cas.application}/serviceValidate</param>
  <param name="filtermapping.cas.1">/*</param>

</config>