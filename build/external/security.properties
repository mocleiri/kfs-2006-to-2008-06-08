dataSource.driverClassName=${driver.class}
dataSource.url=${datasource.url}
dataSource.username=${datasource.username}
dataSource.password=${datasource.password}
dataSource.minSize=2
dataSource.maxSize=50
dataSource.maxWait=10000
dataSource.validationQuery=select 1 from dual
kew.dataSource.driverClassName=${kew.driver.class}
kew.dataSource.url=${kew.datasource.url}
kew.dataSource.username=${kew.datasource.username}
kew.dataSource.password=${kew.datasource.password}
kew.dataSource.minSize=2
kew.dataSource.maxSize=50
kew.dataSource.maxWait=10000
kew.dataSource.validationQuery=select 1 from dual
encryption.key=${encryption.key}
