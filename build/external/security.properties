dataSource.driverClassName=${driver.class}
dataSource.url=${datasource.url}
dataSource.username=${datasource.username}
dataSource.password=${datasource.password}
dataSource.minSize=2
dataSource.maxSize=50
dataSource.maxWait=10000
dataSource.validationQuery=select 1 from dual
workflow.dataSource.driverClassName=${workflow.driver.class}
workflow.dataSource.url=${workflow.datasource.url}
workflow.dataSource.username=${workflow.datasource.username}
workflow.dataSource.password=${workflow.datasource.password}
workflow.dataSource.minSize=2
workflow.dataSource.maxSize=50
workflow.dataSource.maxWait=10000
workflow.dataSource.validationQuery=select 1 from dual
encryption.key=${encryption.key}